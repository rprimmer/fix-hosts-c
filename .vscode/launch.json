{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/fix-hostfiles",
            "args": ["-a", "foo.bar.com"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build my project with clang",
        }
    ]
}
